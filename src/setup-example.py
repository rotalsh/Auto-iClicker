USERNAME = "your-username"
PASSWORD = "your-password"
CLASS_NAME = "class name (caps and space specific)"
CLASS_URL = "copy + paste the class page url (right after you click the course, before join)"
POLL_RATE = 0         # how often the script checks to click a poll response (seconds)
WAIT_JOIN = 0         # how long the script waits until join shows up (seconds)